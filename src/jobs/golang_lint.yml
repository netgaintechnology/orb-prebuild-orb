description: |
  golang linting

executor: golang

parameters:
  lint-dir:
    type: string
    default: .
    description: |
      Directory within to lint all golang files,
      defaults to the working directory (.)

steps:
  - update_alpine_and_checkout
  - run: golangci-lint run <<parameters.lint-dir>>/...
