description: >
  python unit tests

executor: python

parameters:
  test-dir:
    type: string
    default: .
    description: >
      Directory within to lint all python files,
      defaults to the working directory (.)

steps:
  - update_alpine_and_checkout
  - run: |
      cd <<parameters.test-dir>>
      pip install --no-cache-dir pytest -r test_requirements.txt
      python -m pytest -v
